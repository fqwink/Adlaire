# Adlaire Framework — 改訂履歴

> ルールブック改訂履歴を管理する文書。

---

## FRAMEWORK_RULEBOOK.md（フレームワーク仕様）

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| Ver.1.13 | 2026-04-06 | **デュアルデプロイ対応方針を策定**。§10.6「デュアルデプロイ対応方針」を新設。基本方針「1 つのコードベースを変更なしで両ターゲットにデプロイできることを保証」を宣言。4 原則（コードベース単一化・フレームワーク抽象化・ビルド成果物の共通化・自動判定による透過動作）を定義。ターゲット別機能差異表・禁止事項（アプリケーションコード内でのターゲット判定）を追加。§1.6 保留事項「デュアルデプロイ対応方針」を「§10.6 に定義済み」に更新。 |
| Ver.1.12 | 2026-04-06 | §1.6 保留事項から「共用ホスティング — Apache + PHP アプローチ」および「TypeScript → PHP トランスパイラ」を削除（計画白紙）。 |
| Ver.1.11 | 2026-04-06 | §1.6 保留事項「Islands アーキテクチャ」を「計画なし（対象外）」から「未決定」に戻す。 |
| Ver.1.10 | 2026-04-06 | **UI レンダリング層・Islands アーキテクチャを対象外に確定**。§1.6 保留事項の「UI レンダリング層（Preact / JSX / その他）」「Islands アーキテクチャ（採用可否・仕様）」を「計画なし（対象外）」に変更。 |
| Ver.1.9 | 2026-04-06 | **TypeScript ソース実行 = Deno 専用を明確化 / Adlaire Static CMS 採用対応**。§1.4 強制事項テーブルに「TypeScript ソース実行: Deno ランタイム専用」行を追加し、コンパイル済み JS はブラウザ・共用サーバを含む別扱いである旨を注記。§2 技術スタックに「TypeScript ソース実行: Deno ランタイム専用」行を追加。§1.5 適用範囲の「Adlaire Static CMS 管理画面（将来的な移行）」を「採用対象（§10.3 SPA 出力により適用）」に変更。§10.3 を全面改訂：出力モードを「ブラウザ SPA（`--mode=spa`）」と「共用サーバ（`--mode=server`）」の 2 モードに分割定義。ブラウザ SPA 出力は HTTP サーバー層を含まないクライアントサイドルーティングとし、Adlaire Static CMS 管理画面 SPA への適用を明記。§10.4 の `"js"` 行にモード指定説明を追加。§11 CLI の `adlaire build --target=js` を `--mode=spa` / `--mode=server` に分割。§1.6 保留事項の共用ホスティング JS 行を 2 モード対応記述に更新。 |
| Ver.1.8 | 2026-04-06 | **コンパイル済み JavaScript を共用サーバでも動作可能なケースとして定義**。§2 デプロイターゲットから「Deno ランタイム専用」制限を撤廃し「共用サーバを含む」に変更。§10.3 を改訂：「Deno ランタイム上のみで動作する」制限を削除し、JavaScript 実行環境が利用可能な共用サーバでも動作するケースを可能にすると明記。§10.3 に「共用サーバ動作の前提条件」表を追加（JavaScript 実行環境の要件 / 他ランタイム汎用対応との区別）。§1.6 保留事項に「共用ホスティング — コンパイル済み JS によるアプローチ（§10.3 定義済み）」を追加し「Apache + PHP アプローチ」と分離。 |
| Ver.1.7 | 2026-04-06 | **TypeScript からコンパイルした JavaScript の対応を追加（Deno ランタイム専用）**。§2 技術スタックに「ビルド出力」行（TypeScript ソース / コンパイル済み JavaScript・Deno ランタイム専用）を追加。§4 `AdlaireConfig.deploy` 型に `"js"` を追加。§10 デプロイ対応に §10.3「コンパイル済み JavaScript デプロイ」を新設（`build/js/` への出力・**Deno ランタイム専用**・Node.js 等の他ランタイム対応は計画しない・型安全検証はコンパイル前に実施）。§10.3〜§10.4 を §10.4〜§10.5 に再採番。§11 CLI に `adlaire build --target=js` コマンドを追加。 |
| Ver.1.6 | 2026-04-06 | **絶対原則のみを柱とした構成に整理**。§1.1 概要を「3 つの絶対原則を柱とするフレームワーク」として再記述し絶対原則一覧表を追加。不要項目を削除：§1.5 設計の柱（TypeScript 単一ソース / Adlaire Group 専用）・§11 TypeScript → PHP トランスパイラ未確定プレースホルダー・§14 開発フェーズ（ロードマップはフレームワーク仕様ではないため）。CLI コマンドから `adlaire transpile --dry-run`（未確定 PHP 変換）を削除。セクション番号を再採番（§11 CLI / §12 インポート規則 / §13 最終規則 / §14 関連文書）。§13.3 絶対原則参照番号を §1.2〜§1.4 に修正。 |
| Ver.1.5 | 2026-04-06 | **絶対原則§1.4 の文言修正**。「Deno Deploy」→「Deno ランタイムベースの Deploy」に統一。§1.4 絶対原則本文・強制事項テーブル・デプロイターゲットテーブル・§10 デプロイ対応の全該当箇所を更新。 |
| Ver.1.4 | 2026-04-06 | **Deno ランタイム・デュアルデプロイ対応を絶対原則に確立**。§1.4「絶対原則: Deno ランタイム」を新設。「Adlaire Framework は Deno ランタイムで動作する。Deno Deploy および Adlaire Deploy の双方に対応する」を絶対原則として明記。強制事項（実行ランタイム: Deno 2.x / HTTP サーバー: Deno.serve / デプロイターゲット: Deno Deploy + Adlaire Deploy 双方保証 / 標準ライブラリ優先）を規定。§10 デプロイ対応に §1.4 絶対原則との対応注記を追加。§1.5 設計の柱・§1.6 適用範囲・§1.7 保留事項に再採番。 |
| Ver.1.3 | 2026-04-06 | **npm 全面禁止を絶対原則に昇格**。§1.3「絶対原則: npm 全面禁止」を新設。「Adlaire Framework を採用したとき、npm: プレフィックスのインポートを全面禁止する。いかなる例外も認めない」を絶対原則として明記。許可形式（jsr: / deno.land/x / 相対インポート）と禁止形式（npm:* 全形式）を規定。§1.4 設計の柱から「ゼロ npm」を絶対原則に昇格。§13.1 インポート規則を §1.3 絶対原則への参照に更新。 |
| Ver.1.2 | 2026-04-06 | **型安全絶対原則の確立**。§1.2「絶対原則: 型安全」を新設。「型安全はフレームワークが決める。開発者が決めることはできない」を絶対原則として明記。強制事項（ルートパラメータ・ハンドラー引数・ミドルウェア状態・レスポンス）と禁止事項（`any` 使用・強制キャスト・`ctx.params` 手動型定義・`defineHandler` 未経由・`@ts-ignore`）を規定。§1.3 設計の柱を再編（型安全ファーストを絶対原則に昇格させ設計の柱から移動）。§6.1 設計思想に絶対原則§1.2 との対応を明記。 |
| Ver.1.1 | 2026-04-06 | **絶対原則①②の白紙化**。共用ホスティング対応（絶対原則①）・PHP 自己完結生成（絶対原則②）を撤廃し §1.4 保留事項（未決定）に移動。§1.2 絶対原則セクション削除。§1.3 設計の柱からデュアルデプロイ・ファースト・PHP 変換可能 API 設計を削除。§2 技術スタックから PHP/トランスパイル行を除去。§3.1 ディレクトリ構成から transpiler/ を除去。§10 デプロイ対応から PHP 共用ホスティング節・`"php"` ターゲットを削除。§11 を「未確定」保留節に置き換え（仕様白紙）。CLI コマンドから `--target=php`・`adlaire transpile --dry-run` を削除。開発フェーズ Phase 12（PHP トランスパイラ）・Phase 13（PHP デプロイ）を「未確定」に変更。 |
| Ver.1.0（ゼロベース再設計） | 2026-04-06 | **設計思想からの全面改訂**。Ver.1.0〜1.4 を廃棄し新規策定。コアアイデンティティを「Adlaire Group 全プロジェクト共通の TypeScript 製 Web フレームワーク」として再定義。§1.2 絶対原則（①共用ホスティング対応 / ②PHP 自己完結生成）を独立セクションとして確立。§1.3 設計の柱（TypeScript 単一ソース / デュアルデプロイ・ファースト / PHP 変換可能 API 設計 / 型安全ファースト / ゼロ npm / Adlaire Group 専用）を新設。セクション構成を全面再編（§1〜§16、重複・欠番なし）。§11 TypeScript → PHP トランスパイラを Framework コアとして正式位置付け（4 レイヤーパイプライン・PHP ネイティブインライン展開・自己完結ルーター・外部依存ゼロ）。旧 Ver.1.x の Fresh 比較表・php-runtime/・重複セクション番号・開発中断注記等を全て廃棄。 |
| ── 旧バージョン履歴（参考） ── | | |
| Ver.1.4 | 2026-04-06 | **§12 トランスパイラ全面改訂（PHP 自己完結生成・外部依存ゼロ設計）**。絶対原則②「PHP 自己完結生成」を §1.1・§1.2 に追加。`php-runtime/` を廃止し外部依存ゼロを絶対原則として確立。§3.1 トランスパイラディレクトリを 4 レイヤーパイプライン構成（`layers/strip_types.ts`・`prefix_vars.ts`・`api_map.ts`・`syntax.ts`・`validator.ts`）に改訂。§12 を全面書き直し：§12.1 設計原則（自己完結生成原則）・§12.2 変換対象と制約（ビルド時チェック表）・§12.3 4 レイヤーパイプライン・§12.4 変換ルール（Layer 1〜4 全ルール表）・§12.5 ルーティング変換（自己完結 index.php・ルートテーブル・.htaccess）・§12.6 ビルド出力構造（`_runtime/` 廃止）・§12.7 ビルドコマンド・§12.8 実装方針。Framework API 呼び出しを PHP ネイティブコードにインライン展開する方式を正式仕様として確立。 |
| Ver.1.3 | 2026-04-06 | **共用ホスティング対応を絶対原則として正式指定**。§1.1 概要に「絶対原則: 共用ホスティング対応」ブロックを追加。§1.2 設計原則に「共用ホスティング対応（絶対原則）」を追加し、Apache + PHP 8.3+ 共用ホスティングでの動作保証をいかなる設計変更においても維持しなければならない原則として明記。 |
| Ver.1.2 | 2026-04-06 | **デュアルランタイム正式採用・TypeScript → PHP トランスパイラ正式化**。§1.1 概要を改訂し Deno ランタイムと PHP ランタイムの双方をファーストクラスサポートとして明記。§1.2 設計原則の「マルチデプロイ対応」を「デュアルランタイム」に改称し説明を強化、「TypeScript 単一ソース」の説明に「PHP を手書きしない」を明記。§2 技術スタックにランタイム①②の区分を追加（対等なファーストクラス表記）、PHP トランスパイラ行を「正式採用機能」として明記。§3.1 フレームワーク本体ディレクトリ構成に `src/transpiler/`（`mod.ts`・`ast.ts`・`emitter.ts`・`mapping.ts`・`router_gen.ts`）と `src/transpiler/php-runtime/`（`router.php`・`context.php`・`helpers.php`）を追加。§12 TypeScript → PHP トランスパイラの「計画段階」注記を削除し正式仕様として確定。§14 開発フェーズ（旧 §10・§13）の「開発中断予定」注記を削除。セクション番号の重複（§12 インポート規則 / §12 トランスパイラ）を解消し §13 インポート規則・§14 開発フェーズ・§15 最終規則・§16 関連文書 に再採番。 |
| Ver.1.1 | 2026-04-06 | **TypeScript → PHP トランスパイル機能の仕様策定**。§1.1 概要に PHP トランスパイル機能を追記。§1.2 設計原則に「マルチデプロイ対応」「TypeScript 単一ソース」を追加。§1.3 Fresh 比較表に PHP トランスパイル行を追加。§2 技術スタックに PHP 出力・トランスパイルコマンドを追加。§10.3 共用ホスティング（PHP）デプロイを新設。§10.4 デプロイターゲット設定表を新設（php/deno-deploy/adlaire-deploy/auto）。§11.1 CLI コマンド表に `--target=php`・`adlaire transpile --dry-run` を追加。§12 TypeScript → PHP トランスパイラを新設（概要・変換対象・構文マッピング・API マッピング・JS API マッピング・クラス変換・ルーティング変換・変換スコープ限定事項・ビルドコマンド・実装方針）。§13 開発フェーズに Phase 12（トランスパイラ）・Phase 13（PHP デプロイ対応）を追加、開発中断予定の注記を追加。 |
| Ver.1.0 | 2026-04-06 | 初版: 製品定義・Fresh との比較・技術スタック・ディレクトリ構成（フレームワーク本体・アプリケーション構成）・フレームワーク設定（AdlaireConfig）・ファイルベースルーティング（URL マッピング・優先度・特殊ファイル・HTTP メソッドハンドラー）・型安全システム（Context 型・defineHandler・ミドルウェア状態型付け）・ハンドラー仕様・ミドルウェア仕様・静的ファイル配信・デプロイ対応（Deno Deploy / Adlaire Deploy）・CLI ツール・インポート規則（npm 禁止）・開発フェーズ Phase 0-11 を定義。UIレイヤー・Islands を保留事項として明記 |
